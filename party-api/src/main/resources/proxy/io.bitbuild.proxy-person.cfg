proxy.routeId=PersonProxy
proxy.implementation=servlet
proxy.context=/person
proxy.targetURL={{app.person.host}}/person
proxy.enableSSL=false
proxy.isPrivate={{app.private.proxy}}
proxy.privateHeader={{app.private.header}}
proxy.privateToken={{app.private.token}}
